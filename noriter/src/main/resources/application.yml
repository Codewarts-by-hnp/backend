spring:
  profiles:
    active: test

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: noriter-sonarcloud.cxb87rqx7cpa.ap-northeast-2.rds.amazonaws.com
    username: philsohanse
    password: ${DATASOURCE_PASSWORD}

  jpa:
    properties:
      hibernate:
        format_sql: true
    database: mysql

  config:
    import: classpath:oauth.yml

logging.level:
  org.hibernate.SQL: debug
